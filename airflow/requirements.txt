# dbt packages (pinned for cross-container compatibility with dbt-docs)
dbt-core==1.11.12
dbt-postgres==1.11.0
dbt-metricflow

# Airflow and data engineering packages
psycopg2-binary
pandas
Faker
flower

# Note: airflow-dbt may not be compatible with Airflow 3.1.3
# If needed, check compatibility and install separately
# airflow-dbt

# Note: Do NOT include apache-airflow here - it is already in the base image
# Including it would cause version conflicts
